# Local Git Configuration (not tracked in git)
# Copy this file to ~/.gitconfig and customize for your machine
# This file should include your shared config and any machine-specific settings

[user]
    name = Your Name
    email = your.email@example.com

# Include shared configuration from dotfiles repo
[include]
    path = ~/git_tree/wirrDotFiles/.gitconfig.shared
